<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <title><%= pageTitle %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />
  <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
  <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
  <link rel="manifest" href="/site.webmanifest">
  <style>
    @media print {
      .no-print { display: none !important; }
      body { margin: 5mm; }
    }
    @page { margin: 5mm; }
    th, td {
      border: 1px solid #000 !important;
      padding: 8px;
      text-align: center;
      vertical-align: top;
    }
    .lesson {
      text-align: left;
      margin-bottom: 10px;
    }
    .working-hours-bar {
      font-size: 0.92em;
      color: #555;
      margin-bottom: 0.4em;
    }
    .working-hours-bar .bi-clock {
      font-size: 1.08em;
      vertical-align: -2px;
      margin-right: 0.25em;
    }
    .working-hours-label {
      font-weight: 500;
      margin-right: 0.25em;
    }
  </style>
</head>
<body class="bg-white">
  <div class="container-fluid py-4">
    <div class="no-print mb-4">
      <form method="GET" class="row g-2 align-items-end mb-4">
        <div class="col-md-3">
          <label for="fromDate" class="form-label">Od:</label>
          <input type="date" name="fromDate" id="fromDate" value="<%= fromDate %>" class="form-control" />
        </div>
        <div class="col-md-3">
          <label for="toDate" class="form-label">Do:</label>
          <input type="date" name="toDate" id="toDate" value="<%= toDate %>" class="form-control" />
        </div>
        <div class="col-md-2">
          <a href="/dashboard/schedule/print/<%= teacher._id %>" class="btn btn-outline-secondary w-100">❌ Wyczyść</a>
        </div>
        <div class="col-md-2">
          <button type="submit" class="btn btn-outline-primary w-100">🔍 Pokaż zakres</button>
        </div>
        <% if (weeks && weeks.length > 0) { %>
          <div class="col-md-2">
            <button class="btn btn-outline-dark w-100" onclick="window.print()">🖨️ Drukuj</button>
          </div>
        <% } %>
      </form>
    </div>

    <h4><%= teacher.name %> <%= teacher.surname %></h4>
    <div class="working-hours-bar">
      <i class="bi bi-clock"></i>
      <span class="working-hours-label">Godziny pracy:</span>
      <% 
        let hours = teacher.workingHours;
        if (typeof hours === 'string') hours = [hours];
        if (Array.isArray(hours) && hours.length && hours.some(h => h && h.trim() !== '')) { 
      %>
        <%= hours.filter(h => h && h.trim() !== '').join(', ') %>
      <% } else { %>
        <span class="text-muted">brak danych</span>
      <% } %>
    </div>

    <% if (weeks && weeks.length > 0) { %>
      <% weeks.forEach(week => { %>
        <h5 class="mt-4">Tydzień: <%= week.rangeText %></h5>
        <table class="table align-middle text-center">
          <thead class="table-light">
            <tr>
              <% Object.keys(week.weekDays).forEach(dayNum => { %>
                <th><%= week.weekDays[dayNum] %></th>
              <% }) %>
            </tr>
          </thead>
          <tbody>
            <tr>
              <% Object.keys(week.weekDays).forEach(dayNum => { %>
                <td>
                  <% const lessons = week.schedule[dayNum] || []; %>
                  <% if (lessons.length === 0) { %>
                    <span class="text-muted">Brak</span>
                  <% } else { %>
                    <% lessons.forEach(lesson => { %>
                      <div class="lesson">
                        <strong><%= lesson.startTime %> - <%= lesson.endTime %></strong><br />
                          <i class="bi bi-info-circle me-1"></i>
                          <strong class="text-dark">Opis:</strong>
                          <%= lesson.lessonDescription %><br />
                          <i class="bi bi-building me-1"></i>
                          <strong class="text-dark">Placówka:</strong>
                          <%= lesson.facilityId?.name || 'Nieznana' %><br />
                          <% if (lesson.room) { %>
                            <i class="bi bi-door-open me-1"></i>
                            <strong class="text-dark">Sala:</strong>
                            <%= lesson.room %><br />
                          <% } %>
                          <% if (lesson.notes) { %>
                            <i class="bi bi-card-text me-1"></i>
                            <strong class="text-dark">Notatka:</strong>
                            <%= lesson.notes %>
                          <% } %>
                      </div>
                    <% }) %>
                  <% } %>
                </td>
              <% }) %>
            </tr>
          </tbody>
        </table>
      <% }) %>
    <% } else if (fromDate && toDate) { %>
      <p class="text-muted">Brak danych w wybranym zakresie.</p>
    <% } else { %>
      <p class="text-muted">Wybierz zakres dat, aby wyświetlić grafik.</p>
    <% } %>
  </div>
</body>
</html>
